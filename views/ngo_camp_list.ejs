<!-- view/ngo_camp_list.ejs !-->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NGO Camp's Data</title>
    <style>
        body {
            
            display: flex;
            flex-direction: row;
            justify-content: center;
            font-family: 'Arial', sans-serif;
            background-color: #f0f2f5;
            margin: 0;
            padding: 0;
            display: flex;
            min-height: 100vh;
        }

        .box {
        }

        .container-fluid {
            margin: 20px;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        h1 {
            color: #8f2525;
            text-align: center;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            overflow-x: auto;
        }

        th,
        td {
            padding: 12px;
            border: 1px solid #ecdada;
            text-align: left;
            white-space: nowrap;
        }

        th {
            background-color: #8f2525;
            color: #fff;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .error {
            color: red;
            text-align: center;
            margin-bottom: 20px;
        }

        .btn-back {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #8f2525;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            text-align: center;
            transition: background-color 0.3s;
        }

        .btn-back:hover {
            background-color: #721414;
        }
    </style>
</head>

<body>
    <div class="box">
        <div class="container-fluid">
            <h1>NGO Camp's Data</h1>
            <% if (error) { %>
                <p class="error">
                    <%= error %>
                </p>
                <% } %>
                    <table>
                        <thead>
                            <tr>
                                <th>NGO Name</th>
                                <th>Date</th>
                                <th>Location</th>
                                <th>Time</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% camps.forEach(camp=> { %>
                                <tr>
                                    <td>
                                        <%= camp.organizedBy %>
                                    </td>
                                    <td>
                                        <%= camp.date %>
                                    </td>
                                    <td>
                                        <%= camp.location %>
                                    </td>
                                    <td>
                                        <%= camp.timing %>
                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>

                    </table>
        </div>
    </div>


</body>

</html>